import styled from '@emotion/styled';
import { Styled } from '@mui/material/styles';


export const Box = styled('div')(()=>({
}))
export const PendingTaskcontainer = styled('div')(()=>({
    display:'flex',
    flexDirection:'column',
    gap:'16px'
}))